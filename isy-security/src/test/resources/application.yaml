spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://localhost:11413/auth/realms/testrealm/protocol/openid-connect/certs
      client:
        provider:
          testclient:
            issuer-uri: http://localhost:11413/auth/realms/testrealm
        registration:
          testclient:
            client-id: client-credentials-test-client
            client-secret: supersecretpassword
            authorization-grant-type: client_credentials
isy:
  logging:
    anwendung:
      name: Test
      typ: Tet
      version: 0.0.0
