spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9095/auth/realms/testrealm
      client:
        provider:
          testrealm:
            issuer-uri: http://localhost:9095/auth/realms/testrealm
        registration:
          testclient:
            client-id: client-credentials-test-client
            client-secret: supersecretpassword
            authorization-grant-type: client_credentials
            provider: testrealm
          ropc-client:
            client-id: ressource-owner-password-credentials-test-client
            client-secret: hypersecretpassword
            authorization-grant-type: password
            provider: testrealm
          ropc-client-no-bhknz:
            client-id: ropc-no-bhknz-test-client
            client-secret: superhypersecretpassword
            authorization-grant-type: password
            provider: testrealm

isy:
  security:
    oauth2:
      client:
        registration:
          ropc-client:
            username: admin
            password: admin123
            bhknz: testbhknz
          ropc-client-no-bhknz:
            username: adminNoBhknz
            password: passwordNoBhknz