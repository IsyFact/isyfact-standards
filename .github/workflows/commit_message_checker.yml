name: Commit Message Checker
on:
  pull_request:
    branches:
      - master
      - 'release/**'

jobs:
  check-commit-message:
    uses: IsyFact/isyfact-standards/.github/workflows/commit_message_checker_template.yml@feature/ISY-813_Commit_Message_Checker