name: Commit Message Checker
on:
  pull_request:
    branches:
      - master
      - 'release/**'

jobs:
  CommitMessageChecker:
    uses: IsyFact/isy-github-actions-templates/.github/workflows/commit_message_checker_template.yml@v1.8.0
