# Maven Build: Compile, Test, SonarCloudScan
name: Maven Build
on:
  pull_request:
    branches:
      - 'develop'
      - 'main'
      - 'develop-**'
      - 'support/**'
  push:
    branches:
      - 'develop'
      - 'main'
      - 'develop-**'
      - 'support/**'
    paths-ignore:
      - 'isyfact-standards-doc/**'

jobs:
  Version:
    uses: IsyFact/isy-github-actions-templates/.github/workflows/next_version.yml@v3.0.0

  Maven:
    needs: [Version]
    permissions:
      contents: read
    uses: IsyFact/isy-github-actions-templates/.github/workflows/maven_build_template.yml@v3.0.0
    with:
      version: ${{ needs.Version.outputs.next-version }}
      maven-opts: '-PcentralSnapshot'
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

  DeploySnapshotToCentral:
    if: (github.event_name == 'push' || github.event_name == 'workflow_dispatch') &&
      (github.ref == 'refs/heads/develop' || startsWith(github.ref, 'refs/heads/develop-'))
    needs: [ Version, Maven ]
    uses: IsyFact/isy-github-actions-templates/.github/workflows/maven_deploy_template.yml@v3.0.0
    with:
      version: ${{ needs.Version.outputs.next-version }}
      maven-opts: '-PcentralSnapshot'
      deploy-server-id: central
      sbom: true
      sign: true
      environment: 'Release'
    secrets:
      GPG_PRIVATE_KEY: ${{ secrets.MAVEN_GPG_PRIVATE_KEY }}
      GPG_PASSPHRASE: ${{ secrets.MAVEN_GPG_PASSPHRASE }}
      DEPLOY_SERVER_USER_NAME: ${{ secrets.MAVEN_CENTRAL_USER_NAME }}
      DEPLOY_SERVER_TOKEN: ${{ secrets.MAVEN_CENTRAL_TOKEN }}
