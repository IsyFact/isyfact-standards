name: Semgrep Scan

on:
  pull_request:
    branches:
      - master
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  semgrep-scan:
    permissions:
      contents: read
      security-events: write
    uses: IsyFact/isy-github-actions-templates/.github/workflows/semgrep.yml@v2.5.0
