name: Dependency Review
on:
  pull_request:
    branches:
      - master
      - 'release/**'
  workflow_dispatch:

jobs:
  DependencyReview:
    uses: IsyFact/isy-github-actions-templates/.github/workflows/dependency_review_template.yml@v2.1.1