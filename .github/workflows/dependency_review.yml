name: Dependency Review
on:
  pull_request:
    branches:
      - release/3.x

jobs:
  DependencyReview:
    uses: IsyFact/isy-github-actions-templates/.github/workflows/dependency_review_template.yml@v1.3.0