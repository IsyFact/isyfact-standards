name: Dependency Review
on:
  pull_request:
    branches:
      - master

jobs:
  DependencyReview:
    uses: IsyFact/isy-github-actions-templates/.github/workflows/dependency_review_template.yml@feature/IFS-2079_whitelist_git_commit_id_plugin
